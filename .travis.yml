language: java
matrix:
  include:
    - jdk: oraclejdk8

script: bash ./deploy_github_pages.sh

before_install:
          - "export DISPLAY=:99.0"
          - "sh -e /etc/init.d/xvfb start"

addons:
  apt:
    packages:
      - oracle-java8-installer
